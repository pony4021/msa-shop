apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-event-service
  namespace: shop-msa-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-event-service
  template:
    metadata:
      labels:
        app: payment-event-service
    spec:
      containers:
        - name: payment-event-service
          image: <aws_account_id>.dkr.ecr.ap-northeast-2.amazonaws.com/shop-msa/payment-event-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: shop-msa-app-secrets
                  key: PAYMENT_EVENT_DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: shop-msa-app-config
                  key: REDIS_URL
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: shop-msa-app-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: KAFKA_TOPIC_PAYMENT_CREATED
              valueFrom:
                configMapKeyRef:
                  name: shop-msa-app-config
                  key: KAFKA_TOPIC_PAYMENT_CREATED
            - name: KAFKA_GROUP_ID
              valueFrom:
                configMapKeyRef:
                  name: shop-msa-app-config
                  key: KAFKA_GROUP_ID_PAYMENT_EVENTS
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-event-service
  namespace: shop-msa-app
spec:
  selector:
    app: payment-event-service
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: payment-event-service
  namespace: shop-msa-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: payment-event-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payment-event-service
  namespace: shop-msa-app
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: payment-event-service
