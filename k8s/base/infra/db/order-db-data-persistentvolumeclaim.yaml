apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: order-db-data
  name: order-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
