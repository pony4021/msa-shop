apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: inventory-db-data
  name: inventory-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
