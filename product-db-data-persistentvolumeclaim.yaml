apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: product-db-data
  name: product-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
